import { StyleSheet, Text, View } from "react-native";

const trades = () => {
  return (
    <View>
      <Text>trades</Text>
    </View>
  );
};

export default trades;

const styles = StyleSheet.create({});
