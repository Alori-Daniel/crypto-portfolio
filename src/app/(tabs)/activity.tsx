import { StyleSheet, Text, View } from "react-native";

const activity = () => {
  return (
    <View>
      <Text>activity</Text>
    </View>
  );
};

export default activity;

const styles = StyleSheet.create({});
