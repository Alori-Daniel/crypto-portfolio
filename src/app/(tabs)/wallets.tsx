import { StyleSheet, Text, View } from "react-native";

const wallets = () => {
  return (
    <View>
      <Text>wallets</Text>
    </View>
  );
};

export default wallets;

const styles = StyleSheet.create({});
